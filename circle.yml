machine:
  node:
    version: 5.9.0
general:
  branches:
    ignore:
      - gh-pages
test:
  override:
    - npm test
deployment:
  production:
    branch: master
    commands:
      - npm run coverage
